{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "clang++ -std=c++17 src/*.cpp -lsfml-graphics -lsfml-window -lsfml-system -o bin/a.out; if [[ $? == 0 ]]; then ./bin/a.out; fi",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}